---
"@pungfe/element": minor
---

Replace XTableFlex with XTable in XTableRequestConfig and use `useElementSize` for dynamic height. Expose `scrollBarRef` (replaces `container`) from XTableFlex and `table` ref from XTableRequestConfig. Add `rowClassName`, `rowStyle` props and `rowClick`, `rowDblclick`, `selectionChange` events.
